import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    userItem: {
        padding: 15,
        borderBottomWidth: 1,
        borderBottomColor: "#eee",
    },

    userName: {
        fontSize: 16,
    },

    userItemSelected: {
        backgroundColor: "#007AFF", // azulzinho padrão
    },

    userNameSelected: {
        color: "#fff",
        fontWeight: "bold",
    },
});